# Auditoria ENI-50 — 2026-07-02

## Resumo do pedido

Verificar implementação de foto real (`camera`), GPS real (`geolocator`), hash SHA-256 real (`crypto`), permissões (`permission_handler`), degradação graciosa sem GPS, DI de produção com implementações reais e testes mantendo fakes.

---

## Relatório por critério

### 1. Itens declarados como feitos existem no código

| Item | Veredito | Evidência |
|------|--------|-----------|
| `CryptoHashService` | ✅ CONFERE | `lib/data/device/crypto_hash_service.dart` |
| `GeolocatorLocationSource` | ✅ CONFERE | `lib/data/device/geolocator_location_source.dart` |
| `DeviceCameraSource` | ✅ CONFERE | `lib/data/device/device_camera_source.dart` |
| `InAppCameraPreview` | ✅ CONFERE | `lib/presentation/capture/in_app_camera_preview.dart` |
| DI produção real | ✅ CONFERE | `lib/app/di.dart` `_registerCaptureProduction()` |
| Testes com fakes | ✅ CONFERE | `configureDependenciesForTesting` defaults inalterados |
| Degradação GPS | ✅ CONFERE | `capture_occurrence_service.dart` L29-40, `gpsAvailable` |
| Permissões Android | ✅ CONFERE | `AndroidManifest.xml` CAMERA + LOCATION |

### 2. Aderência ao contrato

| Item | Veredito | Evidência |
|------|--------|-----------|
| (a) Hash SHA-256 do arquivo | ✅ CONFERE | `CryptoHashService.hashFile` — hex 64 chars; teste `crypto_hash_service_test.dart` |
| (b) GPS em `occurrences.latitude/longitude` | ✅ CONFERE | `createOccurrence` com coords nullable; null quando `GeoFix?` ausente |
| (c) Contrato mídia sync | ✅ CONFERE | `SyncPayloadSerializer` inalterado; `mime_type: image/jpeg`; `canonicalStoragePath` em `occurrence_repository.dart` L282-288 |
| `content_hash` local | ✅ CONFERE | Persistido em `attachMedia`; não serializado no JSON de sync (pré-existente) |

### 3. Escopo

| Item | Veredito | Evidência |
|------|--------|-----------|
| Sem áudio/vídeo | ✅ CONFERE | Apenas `image/jpeg` em `DeviceCameraSource` |
| Sem compactação | ✅ CONFERE | Arquivo original copiado sem recompressão |
| Deps declaradas | ✅ CONFERE | `pubspec.yaml` camera, geolocator, permission_handler, crypto |

### 4. Testes

| Item | Veredito | Evidência |
|------|--------|-----------|
| Suite verde | ✅ CONFERE | `flutter test` — 104 passed |
| Hash testável | ✅ CONFERE | `test/data/device/crypto_hash_service_test.dart` |
| Degradação GPS | ✅ CONFERE | `capture_occurrence_service_test.dart` — `returnNull: true` |
| Sem hardware em unit | ✅ CONFERE | Fakes em testes de serviço e widget |

### 5. Fronteiras abstratas

| Item | Veredito | Evidência |
|------|--------|-----------|
| `CameraSource` / `LocationSource` / `HashService` | ✅ CONFERE | Interfaces em `lib/domain/services/`; reais em `lib/data/device/` |

---

## Veredito

Implementação aderente ao escopo fechado ENI-50. Hash, GPS e contrato de mídia estão corretos no código e cobertos por testes unitários. Verificação manual no device G56 permanece pendente para aceite final (Postgres, Storage, permissões reais).
