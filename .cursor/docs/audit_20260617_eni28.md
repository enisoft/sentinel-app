# Auditoria ENI-28 / E10.1 — SyncGateway HTTP

**Data:** 2026-06-17  
**Entrada:** `.cursor/docs/last_executed_task.md`  
**Contrato:** `docs/STATUS_PROJETO.md` (POST `/api/v1/occurrences/sync`), `docs/sentinel-decisoes.md`, `docs/sentinel-scope.md`

## Resumo do escopo verificado

Implementação de `SyncGatewayHttp` + `OccurrenceSyncService` para enviar JSON da fila offline ao backend, confirmar sync apenas por presença do `id` em `data.ids`, tratar erros 401/422/5xx sem loop infinito, e garantir que `reported_by` não vai no payload.

---

## (a) Confirmação por `data.ids`

| Item | Veredito | Evidência |
|------|----------|-----------|
| `markSynced` só quando `id ∈ confirmedIds` | ✅ CONFERE | `lib/data/services/occurrence_sync_service.dart:76-78` — `if (confirmedIds.contains(occurrence.id))` antes de `markSynced` |
| 200 sem id não marca synced | ✅ CONFERE | `occurrence_sync_service.dart:76-79` — branch ausente não chama `markSynced`; ocorrência permanece `json_syncing` |
| Parse de `data.ids` no HTTP | ✅ CONFERE | `lib/data/remote/api_client.dart` — `postOccurrencesSync` retorna `(data['ids'] as List).cast<String>()` |
| Teste: id confirmado → synced | ✅ CONFERE | `test/data/services/occurrence_sync_service_test.dart` — `confirmed id in data.ids marks occurrence synced` |
| Teste: id ausente → json_syncing | ✅ CONFERE | `test/data/services/occurrence_sync_service_test.dart` — `missing id in response keeps occurrence pending` |

---

## (b) Caminhos de erro 401 / 5xx / 422 e retry sem loop

| Item | Veredito | Evidência |
|------|----------|-----------|
| 401 → `signOut()` | ✅ CONFERE | `occurrence_sync_service.dart:81-88` — `e.isUnauthorized` → `_auth.signOut()` |
| 5xx → `recordFailure`, `json_syncing`, `retry_count++` | ✅ CONFERE | `occurrence_sync_service.dart:99-105` + `occurrence_repository.dart:235-257` incrementa `retryCount` |
| 422 → prefixo `validation:` | ✅ CONFERE | `occurrence_sync_service.dart:91-98` — `failedReason: 'validation: ${e.message}'` |
| 422 não reprocessado (sem loop) | ✅ CONFERE | `occurrence_sync_service.dart:138-141` — `_isNonRetryable` skip; teste `422 failure is skipped on next processPending cycle` |
| Retry automático para falhas retryáveis | ✅ CONFERE | `occurrence_sync_service.dart:147-149` — `retry(id)` se `failed`; teste `retryable failure is retried on next cycle` |
| Rede (`SocketException`, `ClientException`) | ✅ CONFERE | `occurrence_sync_service.dart:107-120` |
| Bootstrap propaga 401 do sync | ✅ CONFERE | `app_bootstrap_screen.dart` — `syncResult.unauthorized` retorna sem avançar |

---

## (c) `reported_by` vem do token, nunca do payload

| Item | Veredito | Evidência |
|------|----------|-----------|
| Serializer não inclui `reported_by` | ✅ CONFERE | `lib/data/sync/sync_payload_serializer.dart:21-41` — campos listados sem `reported_by` |
| Gateway usa serializer existente | ✅ CONFERE | `sync_gateway_http.dart:39-40` |
| Teste contrato serializer | ✅ CONFERE | `sync_payload_serializer_test.dart:120` — `expect(json.containsKey('reported_by'), isFalse)` |
| Teste POST body | ✅ CONFERE | `sync_gateway_http_test.dart` + `api_client_test.dart` — assert `reported_by` ausente |

---

## (d) Nenhum teste enfraquecido

| Item | Veredito | Evidência |
|------|----------|-----------|
| Suite completa verde | ✅ CONFERE | `flutter test` → **49 passed** (37 anteriores mantidos + 12 novos) |
| `capture_flow_test` atualizado com motivo | ✅ CONFERE | Injeta `FakeSyncGateway`; expectativa `jsonSyncing` reflete sync pós-confirm (comportamento real, não asserção trivial) |
| Asserções de contrato serializer intactas | ✅ CONFERE | `sync_payload_serializer_test.dart` inalterado nas asserções centrais |
| Novos testes cobrem focos a–c | ✅ CONFERE | `occurrence_sync_service_test.dart` (6 casos), `api_client_test.dart` (4 POST), `sync_gateway_http_test.dart` (1) |

---

## Escopo e fronteiras

| Item | Veredito | Evidência |
|------|----------|-----------|
| Sem TUS / binário | ✅ CONFERE | `sync_gateway_http.dart:22-24` no-op; stub em `ensureStubRemotePaths` |
| `syncCheckIns` fora de escopo | ✅ CONFERE | `sync_gateway_http.dart:44-46` — `UnimplementedError` |
| DI produção vs fake testes | ✅ CONFERE | `di.dart` — `SyncGatewayHttp` prod; `configureDependenciesForTesting` aceita override |
| Contrato backend não alterado | ✅ CONFERE | Nenhum arquivo backend tocado |

---

## Verificação manual G65

| Item | Veredito | Nota |
|------|----------|------|
| Captura → sync no device | 🔍 NÃO DOCUMENTADO | Pendente no ambiente local; passos e query SQL documentados em `last_executed_task.md` |

---

## Veredito

**Pronto para considerar concluído no código e testes automatizados**, com ressalva: verificação manual no device G65 (query Postgres com `reported_by` e `path`) ainda **pendente** — gate de Done completo exige execução no device conforme plano.
