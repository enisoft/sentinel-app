# Auditoria ENI-97 — reported_by + isolamento por operador

**Data:** 2026-07-08  
**Escopo:** carimbar dono na captura, enviar no sync, filtrar lista por uid

---

## Resumo verificado

App passa a gravar `reported_by` (uid) na criação do rascunho, incluir no payload de sync quando presente, e filtrar a lista da aba Ocorrências pelo operador logado. Dados de outro uid permanecem no Drift.

---

## Critérios

| # | Critério | Veredito | Evidência |
|---|----------|----------|-----------|
| 1 | Coluna `reported_by` no Drift + migration v8 | ✅ CONFERE | `lib/data/local/tables/occurrences.dart:31-32`, `lib/data/local/app_database.dart:42,80-82` |
| 2 | Captura carimba uid do operador logado | ✅ CONFERE | `lib/data/services/capture_occurrence_service.dart:40-51,188-196`; `AuthGateway.currentUserId` `lib/domain/gateways/auth_gateway.dart:15-16` |
| 3 | `reported_by` imutável após gravação (update não altera) | ✅ CONFERE | `updateDraft` não inclui `reportedBy` — `lib/data/repositories/occurrence_repository.dart:151-168` |
| 4 | Serializer envia `reported_by` no payload | ✅ CONFERE | `lib/data/sync/sync_payload_serializer.dart:41` |
| 5 | Lista filtra por `reported_by = uid` logado | ✅ CONFERE | `listForOperator` `lib/data/repositories/occurrence_repository.dart:113-119`; `occurrences_tab.dart:76-80` |
| 6 | Dados de outro uid não apagados, só ocultos na lista | ✅ CONFERE | `listForOperator` filtra; `listAll` intacto; teste `home_screen_test.dart` ENI-97 |
| 7 | Legadas sem `reported_by` = órfãs, não atribuídas | ✅ CONFERE | `countOrphanOccurrences` `occurrence_repository.dart:122-127`; órfãs excluídas de `listForOperator`; teste `occurrence_reported_by_test.dart` |
| 8 | Sync queue **não** filtra por dono (B pode subir fila de A com autoria preservada) | ✅ CONFERE | `sync_queue_repository.dart:63-72` inalterado — fila global no device |
| 9 | Testes cobrem stamp, serializer, filtro, troca de operador | ✅ CONFERE | `test/data/repositories/occurrence_reported_by_test.dart`, `home_screen_test.dart` |
| 10 | Aviso UI órfãs (parte 2) | 🔍 NÃO DOCUMENTADO | `countOrphanOccurrences` pronto; banner pendente ENI-97 parte 2 |

---

## Veredito

**Pronto para considerar ENI-97 parte 1 concluída.** Lista isola por uid; captura carimba dono; payload leva `reported_by`; legadas ficam órfãs sem atribuição errada. Ressalva: aviso “N capturas sem dono” fica para parte 2; verificação manual G86 pendente.
