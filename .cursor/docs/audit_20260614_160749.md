# Auditoria read-only — Fundação sentinel-app

**Data:** 2026-06-14  
**Escopo:** verificação read-only da fundação offline-first (sem alteração de código).

## Resumo do pedido de verificação

Auditar a fundação do **sentinel-app** contra o contrato (`docs/sentinel-api-app.md`, `sentinel-backend/sentinel-scope.md`) e o relatório da última tarefa (`.cursor/docs/last_executed_task.md`), cobrindo:

1. Máquina de estados da fila (`local_saved` … `synced`, `failed`), `failed_phase`, `retry_count`, timestamps, transições, registro de falha e query de pendentes — com testes.
2. Schema drift (`occurrences`, `occurrence_media`, `check_ins`) aderente ao contrato de API/escopo.
3. Fronteira de rede: `SyncGateway` apenas contrato, sem HTTP.
4. Escopo limpo: sem câmera, TUS, permissões de runtime ou GPS no `pubspec`/código de app.
5. `flutter test`: execução, contagem e cobertura do núcleo.

---

## 1. Máquina de estados da fila

### Estados e serialização

| Item | Veredito | Evidência |
|------|----------|-----------|
| 6 estados (`local_saved`, `media_uploading`, `media_done`, `json_syncing`, `synced`, `failed`) | ✅ CONFERE | `lib/core/sync/sync_state.dart:1-16` |
| `failed_phase` (`media_uploading` \| `json_syncing`) | ✅ CONFERE | `lib/core/sync/sync_phase.dart:3-16`; colunas `failedPhase` em `lib/data/local/tables/occurrences.dart:21`, `lib/data/local/tables/check_ins.dart:14` |
| `retry_count` | ✅ CONFERE | `lib/data/local/tables/occurrences.dart:22`; `lib/data/local/tables/check_ins.dart:15` |
| Timestamps locais da fila | ✅ CONFERE | `createdLocalAt` `occurrences.dart:23`, `check_ins.dart:16`; `mediaUploadedAt` `occurrences.dart:24`, `check_ins.dart:17`; `syncedAt` `occurrences.dart:25`, `check_ins.dart:18`; `lastAttemptAt` `occurrences.dart:26`, `check_ins.dart:19`; `failedReason` `occurrences.dart:27`, `check_ins.dart:20` |
| `isPending` (≠ `synced`) | ✅ CONFERE | `lib/core/sync/sync_state.dart:28` |

### Transições

| Fluxo | Veredito | Evidência |
|-------|----------|-----------|
| Ocorrência com mídia: `local_saved → media_uploading → media_done → json_syncing → synced` | ✅ CONFERE | Regras `lib/core/sync/sync_state_machine.dart:57-64`; métodos `lib/data/repositories/occurrence_repository.dart:106-194` |
| Ocorrência sem mídia: `local_saved → media_done → …` | ✅ CONFERE | `sync_state_machine.dart:59`; `occurrence_repository.dart:128-148` |
| Check-in: `local_saved → json_syncing → synced` | ✅ CONFERE | `sync_state_machine.dart:60,63`; `lib/data/repositories/check_in_repository.dart:50-91` |
| Falha → `failed` + retry para fase anterior | ✅ CONFERE | `canFail` `sync_state_machine.dart:43-44`; `isRetryTransition` `sync_state_machine.dart:32-41`; `recordFailure`/`retry` `occurrence_repository.dart:196-252`, `check_in_repository.dart:93-146` |
| Rejeição de transições inválidas | ✅ CONFERE | `InvalidSyncTransitionException` em `sync_state_machine.dart:18-24`; testes `test/core/sync/sync_state_machine_test.dart:38-47,87-96,134-143` |

### Query de pendentes

| Item | Veredito | Evidência |
|------|----------|-----------|
| Exclui `synced`, inclui intermediários e `failed` | ✅ CONFERE | `lib/data/repositories/sync_queue_repository.dart:60-71` |
| Ordenação por `createdLocalAt` | ✅ CONFERE | `sync_queue_repository.dart:63,70` |
| Stream reativo `watchPending` | ✅ CONFERE | `sync_queue_repository.dart:25-52` |

### Testes da fila

| Área | Veredito | Evidência |
|------|----------|-----------|
| Máquina de estados (9 testes) | ✅ CONFERE | `test/core/sync/sync_state_machine_test.dart:11-144` |
| Pipeline ocorrência + mídia | ✅ CONFERE | `test/data/repositories/repositories_test.dart:48-77` |
| Falha + retry JSON (ocorrência) | ✅ CONFERE | `repositories_test.dart:96-136` |
| Pendentes (synced excluído, failed incluído) | ✅ CONFERE | `repositories_test.dart:160-199` |
| Falha/retry de check-in | 🔍 NÃO DOCUMENTADO nos testes | Implementado em `check_in_repository.dart:93-146`; sem teste dedicado em `repositories_test.dart` |
| Retry após falha em `media_uploading` | 🔍 NÃO DOCUMENTADO nos testes | Lógica em `sync_state_machine.dart:32-41`; sem teste de integração no repositório |
| `watchPending` (stream) | 🔍 NÃO DOCUMENTADO nos testes | Implementado `sync_queue_repository.dart:25-52`; sem teste |

**Subtotal critério 1:** ✅ CONFERE — núcleo completo e testado; lacunas menores em retry de mídia, falha de check-in e stream.

---

## 2. Repositório drift vs contrato

Referências: `docs/sentinel-api-app.md`, `sentinel-backend/sentinel-scope.md` §3.4, `sentinel-backend/STATUS_PROJETO.md` (payload sync).

### Tabela `occurrences`

| Campo contrato | Campo local | Veredito | Evidência |
|----------------|-------------|----------|-----------|
| `id` | `id` (text/UUID) | ✅ CONFERE | `lib/data/local/tables/occurrences.dart:6` |
| `observable_id` | `observableId` | ✅ CONFERE | `occurrences.dart:16` |
| `category_id` | `categoryId` | ✅ CONFERE | `occurrences.dart:17` |
| `title`, `description`, `status`, `priority` | homônimos camelCase | ✅ CONFERE | `occurrences.dart:7-10` |
| `location` | `location` (nullable) | ✅ CONFERE | `occurrences.dart:11` |
| `latitude`, `longitude` | `latitude`, `longitude` (real nullable) | ✅ CONFERE | `occurrences.dart:12-13` (tipo `real` vs `decimal` PG — aceitável offline) |
| `occurred_at` | `occurredAt` | ✅ CONFERE | `occurrences.dart:14` |
| `resolved_at` | `resolvedAt` | ✅ CONFERE | `occurrences.dart:15` |
| `updated_at` | `updatedAt` (nullable) | ✅ CONFERE | `occurrences.dart:18` |
| `created_at` (payload sync) | — | ⚠️ DIVERGENTE | Contrato `STATUS_PROJETO.md:159`; local não tem `createdAt` de domínio — só `createdLocalAt` da fila (`occurrences.dart:23`) |
| `reported_by` | — | ✅ CONFERE (omitido) | Derivado do token no servidor; ausência local correta |
| `assigned_to` | — | ✅ CONFERE (omitido) | Somente painel web |
| `synced_at` (servidor) | `syncedAt` (marca local pós-sync) | ✅ CONFERE | `occurrences.dart:25`; semântica local conforme escopo §3.4 |
| Campos de fila (`sync_state`, `failed_phase`, `retry_count`, timestamps locais) | presentes | ✅ CONFERE | `occurrences.dart:19-27`; alinhado a `sentinel-scope.md:156` |

### Tabela `occurrence_media`

| Campo contrato | Campo local | Veredito | Evidência |
|----------------|-------------|----------|-----------|
| `id` | `id` | ✅ CONFERE | `lib/data/local/tables/occurrence_media.dart:6` |
| `occurrence_id` | `occurrenceId` (FK cascade) | ✅ CONFERE | `occurrence_media.dart:7-8` |
| `media_type` | `mediaType` | ✅ CONFERE | `occurrence_media.dart:9` |
| `path` (Storage remoto) | `remotePath` | ⚠️ DIVERGENTE (nome) | Contrato/API `path` (`STATUS_PROJETO.md:169`); local `remotePath` `occurrence_media.dart:11` — mapeamento JSON pendente na fase HTTP |
| — (device) | `localPath` | ✅ CONFERE | Campo offline-only `occurrence_media.dart:10` |
| `mime_type`, `original_name`, `size_bytes`, `sort_order`, `duration_seconds` | `mimeType`, `originalName`, `sizeBytes`, `sortOrder`, `durationSeconds` | ✅ CONFERE | `occurrence_media.dart:12-16` |
| `created_at`/`updated_at` (Laravel) | — | ✅ CONFERE (omitido) | `STATUS_PROJETO.md:75` — sem timestamps offline na mídia nesta fase |
| hash SHA-256 (captura) | — | 🔍 NÃO DOCUMENTADO nesta fase | Escopo §3.3 prevê hash; fundação explicitamente sem captura (`README.md:41-46`) |

### Tabela `check_ins`

| Campo contrato | Campo local | Veredito | Evidência |
|----------------|-------------|----------|-----------|
| `id` | `id` | ✅ CONFERE | `lib/data/local/tables/check_ins.dart:6` |
| `latitude`, `longitude`, `accuracy` | homônimos (real) | ✅ CONFERE | `check_ins.dart:7-9` |
| `captured_at` | `capturedAt` | ✅ CONFERE | `check_ins.dart:10` |
| `note` | `note` (nullable) | ✅ CONFERE | `check_ins.dart:11` |
| `user_id` | — | ✅ CONFERE (omitido) | Token no sync (`sentinel-api-app.md:96`) |
| Campos de fila (mesmo padrão de ocorrências) | `syncState`, `failedPhase`, `retryCount`, timestamps | ✅ CONFERE | `check_ins.dart:12-20` |
| `mediaUploadedAt` em check-in | presente | ⚠️ DIVERGENTE (menor) | `check_ins.dart:17` — check-in não tem pipeline de mídia; campo provavelmente inutilizado (herança do padrão de fila) |
| Migration backend `check_ins` | — | 🔍 NÃO DOCUMENTADO | API P4 ainda 🔜 (`sentinel-api-app.md:87-98`); schema local antecipa contrato |

**Subtotal critério 2:** ⚠️ DIVERGENTE com ressalvas menores — núcleo aderente; gaps: falta `createdAt` de domínio em `occurrences`, rename `remotePath`→`path` na serialização futura, `mediaUploadedAt` órfão em `check_ins`.

---

## 3. Fronteira de rede (`SyncGateway`)

| Item | Veredito | Evidência |
|------|----------|-----------|
| Interface abstrata declarada | ✅ CONFERE | `lib/domain/gateways/sync_gateway.dart:2-8` |
| Métodos: upload mídia, sync ocorrências, sync check-ins | ✅ CONFERE | `sync_gateway.dart:3-7` |
| Implementação concreta (HTTP/TUS) | ❌ FANTASMA (esperado) | Nenhum `implements SyncGateway` no repositório; comentário explícito `lib/app/di.dart:21` |
| Registro no DI | ✅ CONFERE (não registrado) | `di.dart:15-19` — só DB e repositórios locais |

**Subtotal critério 3:** ✅ CONFERE — contrato puro, sem rede, conforme combinado.

---

## 4. Escopo limpo (sem câmera / TUS / permissões runtime / GPS SDK)

| Item | Veredito | Evidência |
|------|----------|-----------|
| `pubspec.yaml` — sem camera, image_picker, tus, geolocator, permission_handler, http, dio | ✅ CONFERE | `pubspec.yaml:9-26` |
| Código lib — sem imports/SDK de câmera, TUS, geolocalização | ✅ CONFERE | Busca em `lib/` — zero matches |
| Campos `latitude`/`longitude` no schema | ✅ CONFERE | Modelo de dados para sync futuro (`occurrences.dart:12-13`, `check_ins.dart:7-8`); não é implementação GPS |
| AndroidManifest — só `INTERNET` (template Flutter debug/profile) | ✅ CONFERE | `android/app/src/debug/AndroidManifest.xml:6` — sem CAMERA/LOCATION |
| UI placeholder sem captura | ✅ CONFERE | `lib/main.dart:27-40` |

**Subtotal critério 4:** ✅ CONFERE — escopo da fundação respeitado.

---

## 5. Testes (`flutter test`)

**Comando:** `flutter test` (executado em 2026-06-14)

**Resultado:** `00:00 +16: All tests passed!` (exit 0)

| Arquivo | Testes | Foco |
|---------|--------|------|
| `test/core/sync/sync_state_machine_test.dart` | 9 | Transições, falha, retry, rejeições |
| `test/data/repositories/repositories_test.dart` | 7 | CRUD drift, pipeline sync, falha/retry, fila pendente |

**Cobertura do núcleo:** ✅ CONFERE — não são smoke tests; asserções de estado, timestamps, `retryCount`, `failedPhase`, exclusão de sincronizados na fila.

**Lacunas (não bloqueantes):** check-in `recordFailure`/`retry`, retry pós-falha de mídia, `watchPending` stream — 🔍 NÃO DOCUMENTADO nos testes.

**Subtotal critério 5:** ✅ CONFERE — 16 testes verdes cobrindo máquina de estados e repositórios.

---

## Veredito (2 linhas)

**A fundação offline-first está pronta para ser considerada concluída** como esqueleto layer-first (drift + fila + contrato de rede), com testes sólidos no núcleo.

**Ressalvas:** adicionar `createdAt` de domínio em `occurrences` antes do sync HTTP; planejar mapeamento `remotePath`→`path` no payload; opcionalmente testar falha/retry de check-in e stream de pendentes.
