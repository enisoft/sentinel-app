## Ícone de notificação + builds 1.0.0+3

- `ic_notification` (silhueta branca) gerado a partir de `docs/icon-adaptative-foreground-relato-1024.png` em drawable-*dpi.
- `SyncForegroundService` usa `R.drawable.ic_notification` em vez de `ic_launcher`.
- `version`: `1.0.0+3`; `docs/RELEASE_NOTES.md` atualizado.
- Dev instalado no moto g86: `app-dev-release.apk`.
- Prod AAB: `build/app/outputs/bundle/prodRelease/app-prod-release.aab`.
