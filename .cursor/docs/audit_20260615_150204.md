# Auditoria read-only — Serialização local → payload de sync

**Data:** 2026-06-15  
**Escopo:** verificação read-only do relatório `.cursor/docs/last_executed_task.md` (serialização P3/P4, correções de schema, testes) contra código real e contrato.

## Resumo do pedido de verificação

Auditar se a última tarefa entregou de fato:

1. Campo de domínio `created_at` em `occurrences` (distinto de `createdLocalAt` da fila).
2. Remoção de `mediaUploadedAt` órfão em `check_ins`.
3. `SyncPayloadSerializer` aderente a `POST /occurrences/sync` e `POST /check-ins/sync`.
4. Testes unitários (18 total, sem rede) cobrindo serialização e mantendo os 16 anteriores.
5. Escopo fechado: sem HTTP/TUS/câmera; `SyncGateway` continua abstrato.

Referências: `docs/sentinel-api-app.md`, `sentinel-backend/STATUS_PROJETO.md` (P3), contrato P4 em `sentinel-api-app.md:89-98`.

---

## 1. Itens afirmados como "feitos" existem no código?

### Schema drift v2

| Item | Veredito | Evidência |
|------|----------|-----------|
| `occurrences.createdAt` (domínio, coluna `created_at`) | ✅ CONFERE | `lib/data/local/tables/occurrences.dart:18`; gerado `app_database.g.dart:142-143` |
| `createdAt` populado na criação | ✅ CONFERE | `occurrence_repository.dart:37-38,48` — `domainCreatedAt = createdAt ?? now` |
| `createdLocalAt` permanece separado (fila) | ✅ CONFERE | `occurrences.dart:24`; `occurrence_repository.dart:49` |
| `check_ins` sem `mediaUploadedAt` | ✅ CONFERE | `check_ins.dart:16-19` — após `createdLocalAt` vem `syncedAt`; `app_database.g.dart:1977-1999` (sem coluna) |
| Schema v2 + migration v1→v2 | ✅ CONFERE | `app_database.dart:24-38` — `schemaVersion => 2`; `onUpgrade` recria tabelas se `from < 2` |
| `app_database.g.dart` regenerado | ✅ CONFERE | `created_at` em Occurrence `app_database.g.dart:142`; CheckIns sem `mediaUploadedAt` |

### Serialização

| Item | Veredito | Evidência |
|------|----------|-----------|
| Classe `SyncPayloadSerializer` | ✅ CONFERE | `lib/data/sync/sync_payload_serializer.dart:4-79` |
| Payload ocorrências: wrapper `occurrences` | ✅ CONFERE | `sync_payload_serializer.dart:10-14` |
| Campos snake_case do contrato P3 | ✅ CONFERE | `sync_payload_serializer.dart:22-40` — espelha `STATUS_PROJETO.md:163-176` |
| `path` ← `remotePath` na mídia | ✅ CONFERE | `sync_payload_serializer.dart:48` |
| `created_at` / `updated_at` de domínio | ✅ CONFERE | `sync_payload_serializer.dart:36-39` |
| Exclusão de `reported_by`, `assigned_to`, `synced_at` | ✅ CONFERE | Serializer não inclui; teste `sync_payload_serializer_test.dart:120-124` |
| Payload check-ins: wrapper `check_ins` | ✅ CONFERE | `sync_payload_serializer.dart:60-62` |
| Campos P4 (`id`, coords, `accuracy`, `captured_at`, `note`) | ✅ CONFERE | `sync_payload_serializer.dart:66-73`; contrato `sentinel-api-app.md:91-94` |
| Exclusão de `user_id` e campos de fila | ✅ CONFERE | Serializer não inclui; teste `sync_payload_serializer_test.dart:163-167` |
| `toContractIso8601` UTC sem fração | ✅ CONFERE | `sync_payload_serializer.dart:76-78`; teste usa `…T14:35:00Z` |

### Repositório

| Item | Veredito | Evidência |
|------|----------|-----------|
| `createOccurrence` aceita `createdAt` opcional | ✅ CONFERE | `occurrence_repository.dart:35,37-38` |
| `getMedia` ordenado por `sortOrder` | ✅ CONFERE | `occurrence_repository.dart:100-104` |

**Subtotal critério 1:** ✅ CONFERE — todos os itens do relatório existem no código citado.

---

## 2. Aderência ao contrato

### `POST /occurrences/sync` (P3)

| Campo contrato | Payload serializer | Veredito |
|----------------|-------------------|----------|
| `id`, `observable_id`, `category_id`, `title`, `description`, `status`, `priority` | presentes | ✅ CONFERE |
| `location`, `latitude`, `longitude`, `occurred_at`, `resolved_at` | presentes | ✅ CONFERE |
| `created_at`, `updated_at` | `occurrence.createdAt`, `occurrence.updatedAt` | ✅ CONFERE |
| `reported_by`, `assigned_to`, `synced_at` | ausentes | ✅ CONFERE |
| `media[].path` (não `remote_path`) | `'path': media.remotePath` | ✅ CONFERE |
| `media[].media_type`, `mime_type`, etc. | presentes | ✅ CONFERE |
| Campos de fila (`sync_state`, `created_local_at`, …) | ausentes no JSON | ✅ CONFERE |
| `local_path` na mídia | ausente | ✅ CONFERE |

### `POST /check-ins/sync` (P4)

| Campo contrato | Veredito | Evidência |
|----------------|----------|-----------|
| `check_ins[]` com `id`, `latitude`, `longitude`, `accuracy`, `captured_at`, `note` | ✅ CONFERE | `sync_payload_serializer.dart:65-73`; exemplo `sentinel-api-app.md:91-94` |
| `user_id` omitido | ✅ CONFERE | `sync_payload_serializer_test.dart:163` |

### Ressalvas menores de contrato

| Item | Veredito | Evidência |
|------|----------|-----------|
| `path` pode ser `null` se `remotePath` ainda não preenchido | ⚠️ DIVERGENTE (menor) | `sync_payload_serializer.dart:48` — contrato exige `path` obrigatório no sync (`STATUS_PROJETO.md:169`); sem validação/guarda; esperado que E10 só serialize após upload |
| `duration_seconds: null` explícito na imagem | 🔍 NÃO DOCUMENTADO | Teste inclui `null` (`sync_payload_serializer_test.dart:105`); exemplo backend omite campo na imagem (`STATUS_PROJETO.md:177-186`) — provável aceitação pelo Laravel, não testado end-to-end |
| `createdAt` default = `createdLocalAt` quando não informado | 🔍 NÃO DOCUMENTADO | `occurrence_repository.dart:37-38` — campos distintos, mas mesmo valor por default; teste de distinção existe em `sync_payload_serializer_test.dart:42-44,94` |

**Subtotal critério 2:** ✅ CONFERE com ressalvas menores — aderência ao contrato P3/P4; divergência anterior (`created_at`, `remotePath`→`path`, `mediaUploadedAt` em check-in) **fechada**.

---

## 3. Escopo — nada além do pedido

| Item | Veredito | Evidência |
|------|----------|-----------|
| Sem novas dependências no `pubspec.yaml` | ✅ CONFERE | `pubspec.yaml:9-26` — mesmas deps da fundação |
| Sem HTTP/TUS/câmera/GPS SDK | ✅ CONFERE | `lib/` — zero imports `http`, `dio`, `tus`, `camera`, `geolocator`; `sync_gateway.dart:1` só comentário |
| `SyncPayloadSerializer` não registrado no DI | ✅ CONFERE (escopo) | `di.dart:15-21` — só DB + repositórios; serializer é utilitário puro, não reivindicado no relatório |
| Sem implementação de envio HTTP | ✅ CONFERE | Nenhum `implements SyncGateway` no repo |

**Subtotal critério 3:** ✅ CONFERE — escopo fechado respeitado.

---

## 4. Testes

**Comando:** `flutter test` (executado em 2026-06-15)

**Resultado:** `00:00 +18: All tests passed!` (exit 0)

| Arquivo | Testes | Foco | Veredito |
|---------|--------|------|----------|
| `test/core/sync/sync_state_machine_test.dart` | 9 | Máquina de estados | ✅ CONFERE |
| `test/data/repositories/repositories_test.dart` | 7 | Repositórios + fila | ✅ CONFERE |
| `test/data/sync/sync_payload_serializer_test.dart` | 2 | Contrato P3 (2 mídias) + P4 | ✅ CONFERE |

### Cobertura do comportamento afirmado

| Comportamento | Veredito | Evidência |
|---------------|----------|-----------|
| Ocorrência + 2 mídias → JSON contrato P3 | ✅ CONFERE | `sync_payload_serializer_test.dart:25-133` — mapa completo vs `STATUS_PROJETO.md:163-196` |
| `path` de `remotePath`, não `local_path` | ✅ CONFERE | `sync_payload_serializer_test.dart:100,110,126-131` |
| `created_at` do domínio (≠ `createdLocalAt`) | ✅ CONFERE | `sync_payload_serializer_test.dart:42-44,94` — `createdAt` 14:35 vs `createdLocalAt` 14:36 |
| Ausência campos server-only | ✅ CONFERE | `sync_payload_serializer_test.dart:120-124,163-167` |
| Check-in P4 | ✅ CONFERE | `sync_payload_serializer_test.dart:137-168` |
| 16 testes anteriores mantidos | ✅ CONFERE | Contagem 9+7+2=18; suite anterior era 16 |

### Asserções enfraquecidas?

| Item | Veredito | Evidência |
|------|----------|-----------|
| Testes de repositório/fila inalterados em essência | ✅ CONFERE | Pipeline, falha, retry, pendentes — asserções de estado/timestamps preservadas |
| Única alteração em teste antigo: +1 assert `createdAt` | ✅ CONFERE | `repositories_test.dart:46` — reforço, não enfraquecimento |

### Lacunas (não bloqueantes)

| Item | Veredito |
|------|----------|
| Teste de `path: null` rejeitado ou omitido | 🔍 NÃO DOCUMENTADO |
| Teste de migration v1→v2 preservando dados | 🔍 NÃO DOCUMENTADO (migration destrutiva documentada no relatório) |
| Integração serializer ↔ `SyncGateway` | 🔍 NÃO DOCUMENTADO (fora de escopo E10) |

**Subtotal critério 4:** ✅ CONFERE — 18 testes verdes; comportamento afirmado coberto; nenhum teste enfraquecido.

---

## 5. Fronteiras abstratas

| Item | Veredito | Evidência |
|------|----------|-----------|
| `SyncGateway` interface abstrata | ✅ CONFERE | `lib/domain/gateways/sync_gateway.dart:2-8` |
| Sem implementação concreta | ✅ CONFERE | Nenhum `implements SyncGateway`; `di.dart:21` comentário explícito |
| Serializer produz payload; não envia | ✅ CONFERE | `SyncPayloadSerializer` retorna `Map<String, dynamic>`; sem chamadas de rede |

**Subtotal critério 5:** ✅ CONFERE.

---

## Comparação com audit anterior (2026-06-14)

| Divergência anterior | Status atual |
|---------------------|--------------|
| Falta `createdAt` de domínio em `occurrences` | ✅ Fechada (`occurrences.dart:18`) |
| `remotePath`→`path` só na serialização futura | ✅ Fechada (`sync_payload_serializer.dart:48`) |
| `mediaUploadedAt` órfão em `check_ins` | ✅ Fechada (coluna removida) |

---

## Veredito (2 linhas)

**A tarefa de serialização local → payload de sync está pronta para ser considerada concluída** — schema corrigido, serializer aderente ao contrato P3/P4, 18 testes verdes, escopo e fronteira de rede respeitados.

**Ressalvas:** validar `path` não-nulo antes do envio real (E10); migration v2 é destrutiva (aceitável na fundação); serializer não registrado no DI (ok até o worker de sync consumi-lo).
