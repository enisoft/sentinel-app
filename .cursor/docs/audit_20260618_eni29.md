# Auditoria ENI-29 / E10.2 — Upload TUS resumable (re-audit)

**Data:** 2026-06-18  
**Entrada:** escopo ENI-29 (relato histórico em `.cursor/docs/audit_20260618_eni29.md`); verificação manual atualizada em `.cursor/docs/last_executed_task.md` (ENI-36)  
**Contrato:** `docs/STATUS_PROJETO.md` L107–L114/L263, `docs/sentinel-api-app.md` L129

## Resumo do escopo verificado

Upload TUS real **antes** do POST JSON; interface `MediaUploader`; `tus_client_dart` isolado em um arquivo; fila `media_uploading → media_done → json_syncing`; testes via `FakeMediaUploader`; stub ENI-28 (`ensureStubRemotePaths`) removido; sem FGS/Wi-Fi policy; contrato backend inalterado.

---

## (a) Ordenação — JSON nunca antes da mídia confirmada

| Item | Veredito | Evidência |
|------|----------|-----------|
| Upload TUS antes de `beginJsonSync` | ✅ CONFERE | `occurrence_sync_service.dart:66-112` — `_advanceMedia` completo antes de `_advanceToJsonSync` |
| `markMediaDone` + guard `allMediaUploaded` | ✅ CONFERE | `occurrence_sync_service.dart:208-213` — falha se mídia sem `remote_path` |
| `syncOccurrences` só em `json_syncing` | ✅ CONFERE | `occurrence_sync_service.dart:114-116` — guard `syncState != jsonSyncing` → continue |
| Gateway delega upload ao `MediaUploader` | ✅ CONFERE | `sync_gateway_http.dart:26-27` |
| Teste: sync não chamado após falha parcial de mídia | ✅ CONFERE | `occurrence_sync_service_test.dart:175` — `syncCallCount == 0` no 1º ciclo |
| Teste: upload antes de sync (1 mídia) | ✅ CONFERE | `occurrence_sync_service_test.dart:94-113` — `uploadCallCount == 1` antes de `jsonSyncing` |
| Teste: 2 mídias só synced quando ambas com path | ✅ CONFERE | `occurrence_sync_service_test.dart:116-138` |

---

## (b) Retomada TUS por offset (não reenvia do zero)

| Item | Veredito | Evidência |
|------|----------|-----------|
| Skip mídias com `remote_path` já definido | ✅ CONFERE | `tus_media_uploader.dart:42-43` — `if (item.remotePath != null) continue` |
| `TusFileStore` por `media_id` | ✅ CONFERE | `tus_media_uploader.dart:75-79`, `:112-118` — `_storeDirectoryFor(mediaId)` |
| Retry em `media_uploading` após falha | ✅ CONFERE | `occurrence_sync_service.dart:194-196`, `:208-214` |
| Fake espelha skip de offset | ✅ CONFERE | `fake_media_uploader.dart:40` — `simulateOffsetPreserved && remotePath != null` |
| Teste: 1ª mídia preservada, 2ª retomada no 2º ciclo | ✅ CONFERE | `occurrence_sync_service_test.dart:141-188` — `uploadCallCount == 2`, ambas com path |
| Teste unitário direto de `TusMediaUploader` | 🔍 NÃO DOCUMENTADO | Sem teste de integração mock HTTP TUS; cobertura via fake + device |

---

## (c) owner_id = operador (RLS E6)

| Item | Veredito | Evidência |
|------|----------|-----------|
| JWT operador no upload (não anon/service) | ✅ CONFERE | `tus_media_uploader.dart:36-38`, `:88-91` — `Bearer` de `authGateway.accessToken` |
| Sem `service_role` no app | ✅ CONFERE | grep `lib/` — zero referências a `service_role` |
| 401 upload → signOut | ✅ CONFERE | `tus_media_uploader.dart:101-102`; `occurrence_sync_service.dart:69-76` |
| RLS `owner_id = auth.uid()` no device | ✅ CONFERE | `last_executed_task.md` L87-97 — query `storage.objects` OK, `owner_id` = uid operador (ENI-36 manual) |
| Upload TUS real no device sem "arquivo não encontrado" | ✅ CONFERE | `last_executed_task.md` L65-71 — ENI-36 confirmou TUS + objeto em `sentinel-media` |

---

## (d) Path canônico

| Item | Veredito | Evidência |
|------|----------|-----------|
| Formato `occurrences/{id}/{media_id}.{ext}` | ✅ CONFERE | `occurrence_repository.dart:276-283` — `canonicalStoragePath` |
| Alinhado ao contrato `media[].path` | ✅ CONFERE | `STATUS_PROJETO.md` L263; serializer usa `path` derivado de `remote_path` |
| Metadata TUS `objectName` + bucket | ✅ CONFERE | `tus_media_uploader.dart:23`, `:93-97` — `bucketName: sentinel-media`, `objectName: objectPath` |
| `setRemotePath` após upload OK | ✅ CONFERE | `tus_media_uploader.dart:59` |
| Teste path canônico em `remote_path` | ✅ CONFERE | `occurrence_sync_service_test.dart:105-107` — UUID media → `.jpg` |
| Postgres `occurrence_media.path` no device | ✅ CONFERE | `last_executed_task.md` L73-85 — query OK com path canônico |

---

## (e) Testes — passam, cobrem, não enfraquecidos

| Item | Veredito | Evidência |
|------|----------|-----------|
| Suite completa verde | ✅ CONFERE | `flutter test` → **57 passed** (2026-06-18) |
| Casos ENI-28 intactos (401/422/5xx/retry/ids) | ✅ CONFERE | `occurrence_sync_service_test.dart:202-280` — asserções de fase/retry/signOut mantidas |
| +4 casos TUS/mídia (1 mídia, 2 mídias, falha parcial, 401 upload) | ✅ CONFERE | `occurrence_sync_service_test.dart:94-223` |
| `reported_by` ausente no payload | ✅ CONFERE | `sync_payload_serializer_test.dart:120`; `sync_gateway_http_test.dart:94` |
| Asserções não trivializadas | ✅ CONFERE | testes ainda verificam `syncCallCount`, `uploadCallCount`, `failedPhase`, paths reais |

---

## (f) `tus_client_dart` isolado + DI

| Item | Veredito | Evidência |
|------|----------|-----------|
| Único import da lib em `lib/` | ✅ CONFERE | grep — só `lib/data/remote/tus_media_uploader.dart:5` |
| Interface `MediaUploader` na domain | ✅ CONFERE | `lib/domain/gateways/media_uploader.dart:2-6` |
| Produção: `TusMediaUploader` | ✅ CONFERE | `di.dart:129-136` |
| Testes: override `FakeMediaUploader` | ✅ CONFERE | `configureDependenciesForTesting` L72/L96; `fake_sync_gateway.dart:29-30` |
| Dependência declarada | ✅ CONFERE | `pubspec.yaml:23` — `tus_client_dart: 2.5.0` |

---

## Escopo e fronteiras

| Item | Veredito | Evidência |
|------|----------|-----------|
| Stub ENI-28 removido | ✅ CONFERE | grep `ensureStubRemotePaths` — ausente em `lib/` |
| Sem FGS / Wi-Fi policy | ✅ CONFERE | grep `lib/` — sem WorkManager/FGS/wifi policy |
| `syncCheckIns` HTTP fora de escopo | ✅ CONFERE | `sync_gateway_http.dart:48-49` — `UnimplementedError` |
| Contrato POST JSON inalterado | ✅ CONFERE | `sync_payload_serializer.dart`; `ApiClient.postOccurrencesSync` inalterado no contrato |
| ENI-36 (fake JPEG device) fora do escopo ENI-29 | ✅ CONFERE | `fake_device_camera_source.dart` — follow-up de device; não altera pipeline TUS |

---

## Veredito

ENI-29 **concluído e aderente** ao contrato: ordenação mídia→JSON, path canônico, JWT operador, lib TUS isolada, stub removido, testes sólidos (57 verdes). Verificação manual G65/G56 **agora documentada** (ENI-36) para Storage, Postgres e RLS — ressalva anterior de “done condicionado” **superada**.

Única lacuna menor: sem teste unitário mockado de protocolo TUS HTTP (aceitável — isolamento + fake + evidência device).
